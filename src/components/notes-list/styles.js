import { ui } from 'ui'
import styled from 'styled-components'

const H3 = styled(ui.H3)`
  margin-bottom: 2rem;
  color: var(--text-link);
`

const Article = styled.article`
  padding-bottom: 1rem;
`

export const styles = {
  H3,
  Article
}
